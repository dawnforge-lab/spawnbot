/**
 * Kilo Gateway Commands — stubbed out (not applicable to spawnbot)
 */
export function registerKiloCommands(_useSDK: () => any) {
  // no-op: Kilo Gateway commands (/profile, /teams) are not used in spawnbot
}
